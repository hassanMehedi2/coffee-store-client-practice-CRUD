import React from 'react';
import Navbar from './Navbar';

const Login = () => {
    return (
        <div>
            <Navbar></Navbar>
            <h2>Login now</h2>
        </div>
    );
};

export default Login;